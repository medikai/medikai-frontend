/** Cookie name for client-only demo auth (no server secrets; not for production PHI). */
export const DEMO_SESSION_COOKIE = 'healthos_demo'

/** Max-Age / session lifetime in seconds (24h). */
export const DEMO_SESSION_MAX_AGE_SEC = 60 * 60 * 24
