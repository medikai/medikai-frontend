# Copy to `.env.local` only if you add server-side features later.

# This frontend-only build does not require database or JWT variables.
# Example — analytics or future API base URL:
# NEXT_PUBLIC_APP_URL=http://localhost:3000
